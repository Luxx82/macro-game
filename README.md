# macro-game

Public finance / macro policy game prototype.

## Local development

1. Clone the repository.
2. Start a local static file server from the project root, for example:

   ```bash
   python3 -m http.server 8000
   ```

3. Open `http://localhost:8000` in your browser.
